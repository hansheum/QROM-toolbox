\section{Quantum Online-Extraction, or: How to Learn From QRO Queries}\label{sec:extract}
  \todoenum[rough structure]{
    \item Introduce the Online Extractability technique
    \item From IND-CPA to IND-CCA via the U(check)-transform
  }

  \hhnote{
    With example being CCA security of \U. Might be easier to start from
    OW-(q)PCA instead of adding re-encryption to the decryption oracle. 
    At least part of the reason is that to use the extractability tool, 
    we need the starting point to be a probabilistic PKE (need 
    ``control of the randomness generating oracle''?). So, randomized PKE
    achieving OW-qPCA -> KEM achieving IND-CCA.
  }
