\begin{figure}[t]
	\begin{center}
		\fbox{
			\small
			\nicoresetlinenr
			\begin{minipage}[t]{3.5cm}	
        \underline{\gameHeading $\game 0(\adv)$ -- $\game n(\adv)$}
        \smallskip
				\begin{nicodemus}
          \item $(\pk, \sk) \gets \KG()$
					\item $\advbit \sample \adv^{\Oenc, \RO}(\pk)$
					\item $\pcreturn \bool{\advbit = 0}$
				\end{nicodemus}
			\end{minipage}

			\pchspace
			
			\begin{minipage}[t]{3.5cm}
				\underline{\oracleHeading $\Oenc$}
        \smallskip
				\begin{nicodemus}
    		  \item $m \sample \MSpace$
          \item $\ctxt \gets \Enc_\pk(\mssg)$
          \item $\eK \gets \RO(\mssg)$ \pccomment{$\game 0$}
          \item $\eK_1 \sample \KSpace$ \pccomment{$\game 1$}
          \item $\Cspace \setapp \ctxt$
          \item $\pcreturn (\eK_\chalbit, \ctxt)$
				\end{nicodemus}	
			\end{minipage}

			\pchspace

			\begin{minipage}[t]{4cm}
				\underline{\oracleHeading $\RO(x)$}
        \smallskip
				\begin{nicodemus}
          \item $c \gets \Enc_\pk(x)$ \pccomment{$\game 1$}
          \item $\pcif c \in \CSpace : \red{\bad}$ \pccomment{$\game 1$}
          \item $\pcif \Db[x] = \bot :$ 
          \item   \quad $\Db[x] \sample \bin^m$
          \item $\pcreturn \Db[x]$
				\end{nicodemus}	
			\end{minipage}
		}
	\end{center}
	\caption{\label{fig:cparom-hops}
    Game hops for \autoref{thm:cparom}.
  }
\end{figure}

\begin{proof}
  $\game 0$ is the real game, and $\game 1$ is the ideal game, with an added
  $\bad$ event.
\end{proof}
